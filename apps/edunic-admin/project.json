{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "edunic-admin",
  "projectType": "application",
  "root": "apps/edunic-admin",
  "sourceRoot": "apps/edunic-admin/app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "cache": true,
      "inputs": ["default", "^production", { "externalDependencies": ["next"] }],
      "outputs": [
        "{workspaceRoot}/apps/edunic-admin/.next/!(cache)/**/*",
        "{workspaceRoot}/apps/edunic-admin/.next/!(cache)"
      ],
      "options": {
        "cwd": "apps/edunic-admin",
        "tty": false,
        "command": "next build"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "cwd": "apps/edunic-admin",
        "command": "next dev"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "continuous": true,
      "dependsOn": ["build"],
      "options": {
        "cwd": "apps/edunic-admin",
        "command": "next start"
      }
    },
    "serve-static": {
      "executor": "nx:run-commands",
      "continuous": true,
      "dependsOn": ["build"],
      "options": {
        "cwd": "apps/edunic-admin",
        "command": "next start"
      }
    },
    "build-deps": {
      "executor": "nx:noop",
      "dependsOn": ["^build"],
      "options": {}
    },
    "watch-deps": {
      "executor": "nx:run-commands",
      "continuous": true,
      "dependsOn": ["build-deps"],
      "options": {
        "command": "npx nx watch --projects edunic-admin --includeDependentProjects -- npx nx build-deps edunic-admin"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/eslint.config.mjs",
        "{workspaceRoot}/apps/edunic-admin/eslint.config.mjs",
        "{workspaceRoot}/tools/eslint-rules/**/*",
        { "externalDependencies": ["eslint"] }
      ],
      "outputs": ["{options.outputFile}"],
      "options": {
        "cwd": "apps/edunic-admin",
        "command": "eslint ."
      }
    }
  },
  "tags": []
}