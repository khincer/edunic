{
  "name": "db",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/db/src",
  "projectType": "library",
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx drizzle-kit generate"
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx drizzle-kit migrate"
      }
    }
  },
  "tags": ["scope:shared", "type:db"]
}
